---
description: Project overview and general conventions
globs:
alwaysApply: true
---

## Project Overview
A spaced repetition flashcard app using FSRS algorithm.

## Tech Stack
- Next.js 16 (App Router), React 19
- PostgreSQL + Prisma 7
- ts-fsrs for scheduling
- Tailwind CSS 4, shadcn/ui, Base UI, Lucide Icons
- Biome, TypeScript 5

## Naming Conventions

### Symbols (unchanged)
- React components: `PascalCase`
- Functions/variables: `camelCase`
- Constants: `SCREAMING_SNAKE_CASE`
- Prisma models: `PascalCase` (singular)

### File names by area
- **`components/ui/**`**: Generated/maintained for shadcn/ui — **do not apply** project file-naming rules here; follow shadcn conventions when adding or syncing components.
- **App component modules** (`components/**/*.tsx` except under `components/ui/`): match the primary exported component — **`PascalCase.tsx`**. Co-located tests: **`PascalCase.test.tsx`**.
- **Hooks**: **`hooks/useXxxYyy.ts`** (`use` prefix + camelCase), e.g. `useSession.ts`, `useHintReveal.ts`.
- **Non-component TS modules** (`lib/**`, `app/actions/**`, etc.): multi-word files prefer **`kebab-case.ts`**; short or conventional names at `lib/` root may stay (e.g. `db.ts`, `types.ts`). Utility modules that export JSX helpers but not a primary component may stay kebab-case (e.g. `lib/utils/fill-in-blank-sentence-block.tsx`).
- **`app/` route files**: Next.js defaults `page.tsx`, `layout.tsx`, `route.ts`; other UI modules in `app/` use **`PascalCase.tsx`** when they export components.

## Folder Structure
- Components: `components/[feature]/ComponentName.tsx` (feature folder such as `cards/`; exclude `ui/` from project naming rules)
- Server Actions: `app/actions/[feature].ts`
- DB queries: `lib/db/[feature].ts`
- Types: `lib/types.ts` (or `lib/types/[feature].ts`)
- Hooks: `hooks/useXxxYyy.ts`
- Utils: `lib/utils/[name].ts` (kebab-case for multi-word)

## General Rules
- Do not install new dependencies without asking first
- Do not modify files in `prisma/migrations/`
- Prefer composition over inheritance
- Keep components small and single-responsibility
- Use named exports; default exports only for Next.js page/layout files
